import React from 'react';
import Timer from '../Timer';

const PossessionCounter = ({ color, name }) => {
    return(
        <div className="mt-30">
            <Timer />  
        </div>
    )
}

export default PossessionCounter;